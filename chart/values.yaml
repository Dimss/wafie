image:
  api: docker.io/dimssss/wafie-control-plane:latest
  gateway: image-registry.openshift-image-registry.svc:5000/default/wafie-gateway
postgresql:
  auth:
    postgresPassword: "cwafpg"
    username: "cwafpg"
    password: "cwafpg"
    database: "cwaf"
  primary:
    persistence:
      size: 20Gi
  volumePermissions:
    enabled: false
    securityContext:
      runAsUser: "auto"

  securityContext:
    enabled: false

  shmVolume:
    chmod:
      enabled: false

  containerSecurityContext:
    enabled: false