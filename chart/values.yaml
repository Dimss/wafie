image:
  api: image-registry.openshift-image-registry.svc:5000/default/wafie-control-plane
  gateway: dimssss/wafie-gateway:latest
postgresql:
  auth:
    postgresPassword: "cwafpg"
    username: "cwafpg"
    password: "cwafpg"
    database: "cwaf"
  primary:
    persistence:
      size: 20Gi
  volumePermissions:
    enabled: false
    securityContext:
      runAsUser: "auto"

  securityContext:
    enabled: false

  shmVolume:
    chmod:
      enabled: false

  containerSecurityContext:
    enabled: false